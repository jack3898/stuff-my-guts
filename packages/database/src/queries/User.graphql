query Users {
	users {
		id
		firstname
		lastname
		bio
		country
		tel
	}
}

query User {
	user {
		id
		firstname
		lastname
		bio
		country
		tel
	}
}

mutation Authenticate($email: String!, $password: String!) {
	authenticate(email: $email, password: $password)
}
