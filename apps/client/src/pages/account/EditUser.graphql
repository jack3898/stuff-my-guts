mutation EditUser(
	$currentPassword: String!
	$token: String!
	$newEmail: String
	$newPassword: String
) {
	updateAccount(
		currentPassword: $currentPassword
		token: $token
		newEmail: $newEmail
		newPassword: $newPassword
	)
}
